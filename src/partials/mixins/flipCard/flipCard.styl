@require '../../../main'

#flipcard
  .code
    & > div
      +above(laptop)
        col(1/3)
  .specification
    clear both    

.flipcard1
  flipCard()
  width 300px
  height 300px
  margin 3em auto
  p
    align(both)
    fs(150%)
    font-weight: bold
  & > div
    & > div:first-child
      background yellow
    & > div:last-child
      background teal