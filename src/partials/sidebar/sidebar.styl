@require '../../main'

nav.menu
  background black
  ul
    reset-list()
    a
      color gray
      font-size 2em
      display block
      background url(images/woodc70.jpg)
      border-bottom 4px solid #000
      padding 0.5em
      &:hover
        background darken(yellow, 10%)